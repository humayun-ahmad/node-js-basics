const os = require('os');

// platform
console.log(os.platform());

// CPU arch
console.log(os.arch());

// CPU Core infor
console.log(os.cpus());

// Free Memory
console.log(os.freemem());

// Home directory
console.log(os.homedir());

// Total memory
console.log(os.totalmem());

